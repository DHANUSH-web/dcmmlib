# Agent context for dcmmlib

Start with **[INSTRUCTIONS.md](INSTRUCTIONS.md)**. That file is the full brief for a new session.

This library is the GUI-free DeepCleanMyMac engine. The macOS app lives in the sibling repo `dcmm-desktop`.
